{% extends 'base.html.twig' %}

{% block title %}Hello ThemesController!{% endblock %}

{% block content %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<h2>Formulaire des thèmes</h2>

{# {{form_start(themes_form, {'action': path('addThemes')})}} #}
{{form_start(themes_form)}}



<div class="container">
    <div class="row">
        <div class="col">Nom du themes
        {{ form_widget(themes_form.name) }}
        </div>
    </div>
    <div class="row">
        <div class="col">
        {{ form_widget(themes_form.Sauvegarder) }}
        </div>
    </div>
</div>

{{form_end(themes_form)}}







{% endblock %}
